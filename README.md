# mnist-cnn-handwritten-digits
A CNN model was trained on the MNIST handwritten digit dataset to classify digits 0–9 from 28×28 grayscale images. After normalization and training for 5 epochs, the model achieved about 95.1% test accuracy on the 10,000‑image test set.
